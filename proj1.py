import tkinter as tk

# Create a main window
window = tk.Tk()

# Add a label to the window
label = tk.Label(window, text="Hello, Tkinter!, Tkinter!, Tkinter!")
label.pack()

# Start the Tkinter event loop
window.mainloop()
